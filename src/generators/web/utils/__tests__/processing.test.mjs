import { describe } from 'node:test';

describe.skip('generators/web/utils - processing (skipped)', () => {
  // Tests for `processing.mjs` are omitted because they relied on
  // experimental module mocking. They should be re-added using
  // a stable approach (e.g., dependency injection or test doubles).
});
